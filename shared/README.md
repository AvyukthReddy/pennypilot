# shared

Cross-service code shared between `backend` and `worker` (e.g. Pydantic models,
enums, constants). Empty for now — populated as Phase 1+ features introduce
types that both services need to agree on.
